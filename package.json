{
  "name": "selenium",
  "version": "1.0.0",
  "dependencies": {
    "selenium-webdriver": "^4.0.0-alpha.1"
  },
  "scripts": {
    "babel": "babel",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mocha --require babel-core/register --no-timeouts src/bstack_test.js",
    "start": "npm run build"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-register": "^6.26.0",
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "rimraf": "^2.6.2"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
