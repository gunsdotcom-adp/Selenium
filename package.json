{
  "name": "selenium",
  "version": "1.0.0",
  "dependencies": {
    "selenium-webdriver": "^4.0.0-alpha.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mocha --require babel-core/register --no-timeouts bstack-source.js",
    "start": "npm run build"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-register": "^6.26.0",
    "chai": "^4.1.2",
    "fs": "0.0.1-security",
    "js-yaml": "^3.12.0",
    "mocha": "^5.2.0",
    "rimraf": "^2.6.2",
    "yargs": "^12.0.1"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
